{
  "name": "muu-harbu-shop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && npx prisma db push && node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
